public class CourseRegistrationSystem {
    //düzeltilecek tartışılacak kafamız karıştı ağrıdı hiko bi zaamet
    public void requestCourse(Course course){
        if(student.requestedCourses.size() + enrolledCourses.size() < 5 ){
            requestedCourses.add(course);
        }
        else{
            System.out.println();
        }
    }
}
